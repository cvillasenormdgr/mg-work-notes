## Support Needed (3Ds):
    - For /ape/validation, I created a new field in Airtable, "Custom Next Page Function", since the validation for this page isn't linear and has unique validation. With this, I also created /nextPageFunction to store all custom next page functions, mirrored to AIMER
## Done:
    - Update Prios Slide
    - Weekly System Log Review:
        - Xavier
        - PRIME
        - Shared Telecon
    - ACN:
        - APE:
            - Codeplan onChange function in FormSelect
            - Create getElectiveTests in custom options
            - Add asyncOptions to FormCheckbox; add a loading UI when options are loading
            - Refactor APE handleClinicOnChange
            - Add new field, "Custom Next Page Function"; add new dynamic callback function for next page
            - Create APEEmailValidation in nextPageFunction
            - Add setFieldValue in FormAddress and FormDate
            - Rebase branch to feature/setup-app-router-docker and fix conflicts
            - Fix FormSchedule to be compatible with updated FormSelect
            - Change handleClinicOnChange to include branchId, applicantTestCode, applicantTestRecordId
## To do:
    - SPPH Core
    - APE/PEME App Router
## Google Tasks:
    - 0
    - N/A
